import SectionContainer from "../../shared/SectionContainer";

const Featured = ({ className = "" }: { className?: string }) => {
  return <SectionContainer className={className}>Featured</SectionContainer>;
};

export default Featured;
